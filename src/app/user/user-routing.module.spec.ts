import { UserRoutingModule } from './user-routing.module';

describe('UserRouting.Module', () => {
  it('should create an instance', () => {
    expect(new UserRoutingModule()).toBeTruthy();
  });
});
